import { SocialIconDirective } from './social-icon.directive';

describe('SocialIconDirective', () => {
  it('should create an instance', () => {
    const directive = new SocialIconDirective();
    expect(directive).toBeTruthy();
  });
});
